# Virtualenv (created by install.sh / install.ps1)
.venv/

# Vendored Nutanix v4 swagger specs (reference only, not part of the package)
v4.0 api namespaces/

# Local, environment-specific config (may contain host IPs / secrets)
config.yaml

# Python build / cache artifacts
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# OS cruft
.DS_Store
